package com.realtimefraud.fraud.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class PostgresConfig {
    // TODO: DataSource, JPA config, etc. (later)
}
