package com.realtimefraud.fraud.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisConfig {
    // TODO: Redis connection factory, templates, etc. (later)
}
